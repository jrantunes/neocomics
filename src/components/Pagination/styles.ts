import styled from "styled-components";

export const Container = styled.div`
  display: flex;

  gap: 0.75rem;

  strong {
    color: var(--white);
    font-size: 1.25rem;
    letter-spacing: 0.2px;
  }
`;